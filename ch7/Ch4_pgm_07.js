// Using the findTotal function to display a calculation

var number1 = 1000;
var number2 = 66;
var number3 = 25; // Adding a third number
var result;
var findTotal;

findTotal = function () {
    result = number1 + number2 + number3; // Updating to add three numbers
};

findTotal();

console.log(number1 + " + " + number2 + " + " + number3 + " = " + result); // Updating console.log
